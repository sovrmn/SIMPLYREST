<!-- Commandes en attente de validation -->

<div class="container tovalidate">

  <div class="card-product-header">
    <div class="card-product-num-header">
      <h2> # </h2>
    </div>
    <div class="card-product-supplier-header">
      <h2> Fournisseur </h2>
    </div>
    <div class="card-product-total-header">
      <h2> Total </h2>
      </div>
    <div class="card-product-action-header">
     <h2> Action </h2>
    </div>
  </div>

  <% @orders.each do |order| %>
    <% if order.validated %>
    <% else %>

    <div class="card-product">

      <div class="card-product-num">
        <h2><%= order.order_number %></h2>
      </div>
      <div class="card-product-supplier">
        <h2> <%= order.supplier.name %> </h2>
      </div>
      <div class="card-product-total">
        <h2> <%= order.total %> € </h2>
      </div>

      <div class="card-product-action">
        <button class="button-green-order" type="button" data-toggle="modal" data-target="#Modal<%= order.id %>">
          Valider la commande
        </button>
        <div class="modal fade" id="Modal<%= order.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-backdrop="false">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel"><%= order.order_number %></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <% order.order_items.each do |order_item| %>
                  <p><%= order_item.supplier_item.product.name.capitalize %> <%= order_item.quantity%> <%= order_item.unit %> </p>
                <% end %>
              </div>
              <div class="field">
                <%= simple_form_for order, remote: true do |f| %>
                  <%= f.input :validated, as: :hidden, input_html: { value: true } %>
                  <%= f.submit "Valider la commande", class: "button-modal" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>

      </div>

    </div>

    <% end %>
  <% end %>
</div>

<!-- Commande en attente de reception -->

<div class="container toreceive">

<div class="card-product-header">
    <div class="card-product-num-header">
      <h2> # </h2>
    </div>
    <div class="card-product-supplier-header">
      <h2> Fournisseur </h2>
    </div>
    <div class="card-product-total-header">
      <h2> Total </h2>
      </div>
    <div class="card-product-action-header">
     <h2> Action </h2>
    </div>
  </div>

  <% @orders.each do |order| %>
    <% if order.delivered || (order.validated != true) %>
    <% else %>

    <div class="card-product">

      <div class="card-product-num">
        <h2><%= order.order_number %></h2>
      </div>
      <div class="card-product-supplier">
        <h2> <%= order.supplier.name %> </h2>
      </div>
      <div class="card-product-total">
        <h2> <%= order.total %> € </h2>
      </div>

      <div class="card-product-action">
        <button class="button-green-order" type="button" data-toggle="modal" data-target="#Modal<%= order.id %>">
          Valider la reception
        </button>
        <div class="modal fade" id="Modal<%= order.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-backdrop="false">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel"><%= order.order_number %></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <% order.order_items.each do |order_item| %>
                  <p><%= order_item.supplier_item.product.name.capitalize %> <%= order_item.quantity%> <%= order_item.unit %> </p>
                <% end %>
              </div>
              <div class="field">
                <%= simple_form_for order, remote: true do |f| %>
                  <%= f.input :validated, as: :hidden, input_html: { value: true } %>
                  <%= f.submit "Valider la commande", class: "button-green-order" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>

      </div>

    </div>

    <% end %>
  <% end %>


</div>

<!-- Commande reçu -->

<div class="container received">

  <div class="card-product-header">
    <div class="card-product-num-header">
      <h2> # </h2>
    </div>
    <div class="card-product-supplier-header">
      <h2> Fournisseur </h2>
    </div>
    <div class="card-product-total-header">
      <h2> Total </h2>
      </div>
    <div class="card-product-action-header">
     <h2> Action </h2>
    </div>
  </div>

  <% @orders.each do |order| %>
    <% if order.delivered == false %>
    <% else %>

    <div class="card-product">

      <div class="card-product-num">
        <h2><%= order.order_number %></h2>
      </div>
      <div class="card-product-supplier">
        <h2> <%= order.supplier.name %> </h2>
      </div>
      <div class="card-product-total">
        <h2> <%= order.total %> € </h2>
      </div>

      <div class="card-product-action">
        <button class="button-green-order" type="button" data-toggle="modal" data-target="#Modal<%= order.id %>">
        Afficher le detail
        </button>
        <div class="modal fade" id="Modal<%= order.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-backdrop="false">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel"><%= order.order_number %></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <% order.order_items.each do |order_item| %>
                  <p><%= order_item.supplier_item.product.name.capitalize %> <%= order_item.quantity%> <%= order_item.unit %> </p>
                <% end %>
              </div>
              <div class="field">
                <%= simple_form_for order, remote: true do |f| %>
                  <%= f.input :validated, as: :hidden, input_html: { value: true } %>
                  <%= f.submit "Valider la commande", class: "button-green-order" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>

      </div>

    </div>

    <% end %>
  <% end %>

</div>


